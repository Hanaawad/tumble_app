function showActivities(resultsHTML) {
  const activities = document.getElementById('activity-cards');
  activities.innerHTML = resultsHTML;
}

showActivities('<%= j render "list", activities: @activities %>')

